# 프로젝트 안내 (정보관리기술사 학습 노트)

순수 HTML/CSS/JS 정적 사이트(GitHub Pages). 빌드 도구 없음.

## 구조
- `index.html` (목록·검색), `topic.html` (상세, `?id=` 슬러그)
- `data/topics.json` — 주제 메타데이터(제목·카테고리·태그·요약·updated)
- `content/<id>.md` — 주제별 본문(마크다운, marked + DOMPurify로 렌더)
- `assets/js/main.js`(목록·검색·D-day), `assets/js/topic.js`(상세·이전/다음 네비)
- 렌더러: `assets/js/vendor/`(marked·DOMPurify self-host), mermaid·chart.js는 CDN 정확버전 고정

## 주제 작성 규칙 (중요 — 심화과정 기준)
**앞으로 추가·수정하는 모든 주제는 "논술형(답안형)", 그리고 기술사 2차 필기 심화과정 수준으로 작성한다.** 실제 시험 답안(약 2~3페이지)을
쓸 수 있는 깊이와 분량을 갖춰야 하며, 1교시 단답형 주제도 예외 없이 논술형·심화로 작성한다. "짧고 요약된" 글은 실패로 간주한다 — 항상 **최대한 많이, 최대한 깊게** 채운다.

### 깊이 기준 (표 나열식 "요약 시트" 금지, 분량 미달 금지)
- **표만 나열하지 말 것.** 각 개념·항목은 표 앞뒤로 **산문(문단)으로 원리·이유·배경을 서술**한다.
  표는 비교·정리 보조 수단이며, "왜 그런지"는 문장으로 설명한다.
- **소항목(가/나/다/라)은 각각 최소 3~5문단 + (필요시)표**로 충분히 전개한다. 한두 문장으로 끝내지 않는다.
- **구체 사례·수치·산업 적용 예시**를 섹션마다 최소 1개 이상, 문서 전체 3개 이상 포함한다.
- 비교는 항목 나열이 아니라 **차이가 생기는 이유와 실무적 함의**까지 설명한다.
- **개념도는 최소 2개 이상**(전체 구조도 + 프로세스/아키텍처 세부도) 서로 다른 mermaid 다이어그램으로 표현한다.
- **심화 섹션을 반드시 1개 이상 추가**한다 — 예: 최신 동향/표준 변화, 실무 적용 사례(회사·산업 사례), 관련 기출/유사주제 연계, 예상 출제 방향 및 답안 구성 전략 중 택1 이상.
- 사실관계·최신 동향(버전, 표준 개정, 최근 사례)이 불확실하면 반드시 웹 검색(pplx_sdk)으로 확인하고, 문서 하단에 `## 참고자료`로 실제 URL을 남긴다.
- 결론(고려사항·시사점)은 **기술사 관점**(적용 전략·트레이드오프·전망·연계 기술)으로 심화하며 최소 4개 항목 이상 작성한다.
- 목표 분량: **본문 150줄 이상**(단순 개념 주제도 120줄 이상). **작성 후 반드시 `wc -l content/<id>.md`로 분량을 확인**하고, 목표에 못 미치면 얇은 섹션을 보강해 다시 채운 뒤에만 커밋한다.

### 구조
1. `# 제목`
2. `## 1. 개요` — 정의(> 인용구) + 등장 배경/필요성 **산문 서술** + (필요시) 특징
3. `## 2~4.` — 개념도(mermaid 2개 이상) + **각 요소를 문단으로 설명** + 표(보조) + 유형·절차·구성요소
   - 원 문제에 소문항(가/나/다/라)이 있으면 **모두, 깊이 있게 커버**
4. 비교 / 사례 (이유·맥락 서술 포함, 구체 사례·수치 포함)
5. `## N. 심화` — 최신 동향/실무 사례/기출 연계/예상 출제 방향 중 1개 이상
6. `## N+1. 고려사항 및 시사점` — 기술사 관점 심화 (4개 항목 이상)
7. (필요시) `## 참고자료` — 근거로 사용한 실제 URL 목록
8. `---` 뒤 `> **한 줄 요약**: ...`

- 표(`|...|`)와 mermaid 다이어그램을 적극 활용하되 **산문 설명과 병행**(정의는 `>` 인용구로).
- 정확성 우선: 불확실하면 웹 검색으로 확인하고, 그래도 불확실하면 일반화하고 단정 표현을 피한다.

## 주제 추가 절차
1. `content/<id>.md` 작성(위 구조), `id`는 소문자 슬러그(`^[a-z0-9][a-z0-9-]*$`)
2. `data/topics.json`에 항목 추가(id·title·category·tags·summary·updated)
3. `id` 중복 금지, `content/<id>.md`와 1:1 정합 유지
4. 기출 주제는 태그에 회차(예: `133회`) 포함

## 검증
- JSON 유효성 + `content/<id>.md` 정합성 확인
- 로컬 서버(`python3 -m http.server`)로 렌더 확인(표·mermaid·이전/다음 네비)

## 카테고리(예)
프로젝트·조직관리 / 경영·사업전략 / AI·데이터 / SW공학·관리 / 데이터베이스 /
네트워크 / 컴퓨팅·임베디드 / 인프라·클라우드 / 보안·개인정보 / 하드웨어·반도체
